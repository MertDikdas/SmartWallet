apiVersion: apps/v1
kind: Deployment

metadata:
  name: budget-postgres

spec:
  replicas: 1

  selector:
    matchLabels:
      app: budget-postgres

  template:
    metadata:
      labels:
        app: budget-postgres

    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine

          ports:
            - containerPort: 5432

          env:
            - name: POSTGRES_DB
              value: smartwallet_budget

            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: budget-secret
                  key: BUDGET_DB_USERNAME

            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: budget-secret
                  key: BUDGET_DB_PASSWORD

          volumeMounts:
            - name: postgres-storage
              mountPath: /var/lib/postgresql/data

      volumes:
        - name: postgres-storage
          persistentVolumeClaim:
            claimName: budget-postgres-pvc
